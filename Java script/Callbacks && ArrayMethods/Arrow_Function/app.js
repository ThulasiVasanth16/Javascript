//const add = (x,y) => {
    //return x + y;
//}

//single parameter is optional for paranthese
const square = (x,y) => {
    return x * x;
}

//const rollDie = () => {
  //  return Math.floor(Math.random() * 6) + 1
//}

const rollDie = () => (
     Math.floor(Math.random() * 6) + 1
)

const add = (a,b) => a+b